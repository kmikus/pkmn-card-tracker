{
  "name": "server",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "npm run switch:dev && npm run build && npm start",
    "dev:prod-db": "npm run switch:prod && npm run build && npm start",
    "switch:dev": "cp .env.development .env",
    "switch:prod": "if [ -z \"$PROD_PASSWORD\" ]; then echo '❌ PROD_PASSWORD not set. Set it with: export PROD_PASSWORD=\"your_password\"'; exit 1; fi && cat .env.production | sed \"s/<PASSWORD>/$PROD_PASSWORD/g\" > .env",
    "test": "echo \"Error: no test specified\" && exit 1",
    "test-db": "node dist/test-db.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@prisma/client": "^6.12.0",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "passport": "^0.7.0",
    "passport-google-oauth20": "^2.0.0",
    "prisma": "^6.12.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.3",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/node": "^24.0.10",
    "@types/passport": "^1.0.17",
    "@types/passport-google-oauth20": "^2.0.16",
    "@types/pg": "^8.15.4",
    "typescript": "^5.8.3"
  }
}
